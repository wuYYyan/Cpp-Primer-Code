#include <stdlib.h>

int main()
{
    int x = 10, y = 3, z;

    printf("%d\n", z = (x % y, x / y)); //输出3，会提示逗号左边的表达式对最终结果无影响
    printf("%d\n", z = (y--, x / y)); //输出5，经过左边表达式的计算y的值已被修改
    //逗号运算符规定了从左到右依次求值，然后将左边表达式的求值结果丢弃
    //注意左边表达式的某些变量改变影响右边表达式的求值
    return 0;
}